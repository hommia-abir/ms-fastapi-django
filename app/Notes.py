# what jinja actually does is it converts html files into strings so that u can return them
# via the get and post methods of ur app variable
#whenever u need to reference the request on the view u must import Request from fast api !
